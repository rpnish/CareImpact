# MongoDB Atlas connection URI (leave blank to run in local in-memory mode)
# Example Atlas URI: mongodb+srv://<username>:<password>@cluster0.abcde.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=
DB_NAME=medicare_star_ratings

# Path to the source CSV file
DATA_CSV_PATH=../data/data.csv
